export enum Result {
  True = 'True',
  False = 'False',
}

export enum Product {
  Movie = 'movie',
  Series = 'series',
  Episode = 'episode',
}

export enum Plot {
  Short = 'short',
  Full = 'full',
}
